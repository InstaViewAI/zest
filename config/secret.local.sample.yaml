# Copy to config/secret.local.yaml and fill it in. secret.local.yaml is gitignored.
# Anything here overrides the same key in config/config.local.yaml.
slack:
  token: xoxb-...
zendesk:
  email: you@instaview.ai
  api_token: "..."
  # Empty skips webhook signature verification — fine locally, never in prod.
  webhook_secret: ""
